services:
  frontend:
    build:
      context: .
      target: dev
    restart: unless-stopped
    environment:
      NODE_ENV: development
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules

networks:
  default:
    name: media-watchlist-network
    external: true
